> ## ⚠️ 이 repo에서 병행 작업 중인 분께 (중요)
> 여러 명이 **같은 co-writer-bot repo를 동시에** 작업 중입니다. `git add -A` / `git commit -a` /
> `git commit -am`을 쓰지 마세요 — **남의 진행 중 변경(+새 파일 누락)을 자기 커밋에 쓸어담아** 사고가 납니다.
> 실제로 2026-07-09 스토리보드용 `app.py`·`prompts.py` 편집이 트렌드/피드백 커밋에 딸려 들어가고,
> 신규 `bot/video_guide.py`는 untracked라 누락돼 **HEAD의 import가 깨진 채 push**됐습니다(c82fe82로 복구).
> → **항상 `git add <내가 만진 파일>`로 명시 스테이징하고, `git status`로 확인 후 커밋하세요.**

# 핸드오프 — [스토리보드] 기능 (2026-07-09)

대본 → 숏폼(9:16 · 화당 ~90초 · 다인물) 스토리보드를 슬랙에서 만드는 기능.
`[변환]` 핸들러와 같은 골격 + 스레드 후속 = 수정 흐름(`_do_revise`) 재사용.

## 2단계 흐름 (같은 스레드, 후속 자연어로 수정 · 수정 시 "바뀐 부분만" 출력)

> ⚠️ 예전 '작가 확인용 화면쉽게(6칸)' 중간단계는 **폐기**(실무자가 GPT 이미지 결과로 판단하지 텍스트로 확인 안 함).

1. **씬 설계** — `[스토리보드] <작품> N화` → (입력: 붙여넣기 → **시트 대본** → 스레드 직전 봇 출력) → 씬 분할 + 시간만.
   피드백 loop. 스레드에 `생성`(SB_GEN_RE: 생성/콘티/ㄱㄱ 등) 치면 2단계로.
2. **상세 콘티 (GPT 이미지 생성용)** — 대본을 **샷 단위 연속 산문**으로. 각 샷에 프레임에 잡히는 것(괄호)·카메라 무빙·대사 원문·나레이션 처리(입 안 움직임)·연기 뉘앙스 명시. 이 콘티를 GPT 이미지에 넣어 그림 콘티(스틸) 생성.
   - **★최우선 = 대본 내용 불변**(사건·행동·순서·대사 안 바꿈, 대사 원문). 실무자 최대 불만이 "대본과 달라짐".
   - 정본 예시 = `video_guide.py §3`(실무자가 GPT 이미지로 가장 만족한 EP01 연우방 콘티).
   - (그 다음 단계 = GPT 이미지 → 씨댄스 영상. 봇 밖/추후.)

## 파일

- `bot/video_guide.py` — 가이드 SSOT(`VIDEO_GUIDE` 삼중따옴표 상수, v2). 교체는 여기만. **본문에 삼중따옴표 금지**(백틱·일반따옴표 안전).
  - 0.대본불변 → 1.상세콘티=샷단위연속산문(명시사항 5개) → 2.프레이밍·카메라 문법 → 3.정본예시(EP01 연우방) → 4.셀프체크.
- `bot/prompts.py` — `storyboard_plan_system`(1단계 씬설계) / `storyboard_system`(2단계 **상세 콘티**) / `storyboard_bible_block`(작품 바이블 주입, 개요는 안 넣음) / `STORYBOARD_FEEDBACK_PRINCIPLE`(문외한 자연어 해석). 가이드 비면 `RuntimeError`.
- `app.py` — `CMD_STORYBOARD`, `_do_storyboard`(1단계 + 시트 대본 자동 인식 `_sb_script_from_bible`), `_do_revise`의 `sb` 모드(생성/설계수정/콘티수정 분기, 2단계엔 원본 대본 재주입 `sb_ref_block`), `_sb_stage`(배지 `[1단계]`/`[2단계]`로 감지), `SB_GEN_RE`, 배지 `SB_BADGE_PLAN/BOARD`.

## 특징

- **문외한 피드백 해석**: "씬4 임팩트 좀" 같은 막연한 요청 → 의도 해석해 구체 반영 + `바꾼 점:` 한 줄로 쉽게 설명.
- **작품 바이블(노션→시트 동기화분) 물림**: `<작품>` 주면 금지사항·현재화/해당화 개요·로그라인·핵심정서·타겟·회차분배·등장인물 주입(`storyboard_bible_block`). `<작품>` 없으면 대본만으로.
- **수정 = 바뀐 씬만** 출력(전체 재출력 금지), 1·2단계 공통.

## ⚠️ 커밋 사고 메모 (2026-07-09)

병행 세션이 `git commit -am`으로 스토리보드용 `app.py`·`prompts.py` 편집을 트렌드/피드백 커밋(9c8039c·f8d6bea·d2ac4e6 등)에 **쓸어담아** 커밋함. 그런데 신규 `bot/video_guide.py`는 untracked라 누락 → HEAD가 `from .video_guide import VIDEO_GUIDE`를 하는데 파일이 없어 **import 깨짐**. 이 커밋에서 `video_guide.py` 추가 + 나머지 정리로 복구. **교훈: 같은 repo 병행 작업 시 `git add -A`/`commit -a` 금지, 내 변경분만 명시 스테이징.**

## 남은 일

- 슬랙 실기동 테스트(토큰 필요).
- 3단계(씨댄스) 착수 — 위 목표 산출물·파이프라인 확인 후.
