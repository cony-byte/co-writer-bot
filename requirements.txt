slack-bolt>=1.20.0
claude-agent-sdk>=0.1.0   # agent 백엔드 (기본) — Claude Code 팀 로그인 재사용
anthropic>=0.92.0         # api 백엔드 (COWRITER_BACKEND=api일 때만 사용)
Pillow>=10.0.0            # [이미지] 스토리보드 그리드 합성·캡션 렌더 + face_grid 얼굴 격자 오버레이
opencv-python>=4.8,<5     # [영상] face_grid 이미지 디코드(bytes→ndarray) — headless 빌드는
                          # ultralytics가 임포트 시 요구하는 cv2.imshow가 없어 임포트 자체가
                          # 깨짐(실측 확인, 2026-07-22) — GUI 포함판 필수, headless로 바꾸지 말 것
numpy>=1.24              # [영상] opencv 디코드용
ultralytics>=8.0          # [영상] face_grid 사람 감지(YOLOv8n, 안전필터 자동 대응) — 가중치는 bot/models/yolov8n.pt
pycapcut>=0.0.3          # [내보내기] CapCut 프로젝트(draft) 생성 — 자동 합본과 별개, 정밀 편집 핸드오프용
                        # (pymediainfo 네이티브 필요, darwin 동작 확인 2026-07-22 / uiautomation은 win 전용 가드)
