# 봇 적용 핸드오프 — 필터/트렌드 복구 (2026-07-09)

## 한 줄 요약
신규 편(0706·0707·BL 40·whisperX 6)이 봇 트렌드/필터에 안 잡히던 문제 해결.
레퍼런스 사본 갱신 + `cats`→봇 레거시 필드 백필로 **일반 풀 21→92, BL 풀 0→40**.

---

## 무엇이 바뀌었나

### 1) 레퍼런스 사본 (`data/reference/`)
- `sync_reference.py`로 story-v1-scripts 최신본 복사 → **137편**
- `bl_cats.json` **신규 포함** (없으면 BL 풀이 0이 됨)
- 커밋: `af69d0a` (master, push 완료)

### 2) 태깅 파이프라인 (story-v1-scripts `retag_v4.py`)
봇이 게이트/필터에 쓰는 필드를 `cats`에서 역산 백필하도록 추가(`backfill_bot`):
- 관계·소재·남주 → `tags.trope_tags_ko`
- 분위기·관계·남주 → `tags.catharsis_type`
- `v4_tagged=true` + `v4_tag_confidence` (HAND 큐레이션편=0.7, 자동편=기존)
- **기존 값 보존**(비파괴), **BL은 제외**(bl_pool로 별도 처리)

> 봇 코드(`bot/*.py`)는 **수정 안 함**. 게이트·필터 로직 그대로.

---

## 적용 절차

1. **최신 pull** (배포가 GitHub에서 당기는 구조면)
   ```
   git pull   # master, af69d0a 포함
   ```
   로컬 실행이면 사본은 이미 갱신됨.

2. **레퍼런스 재동기화** (story-v1-scripts를 새로 갱신한 경우에만)
   ```
   python3 scripts/sync_reference.py
   git add data/reference && git commit && git push
   ```

3. **봇 캐시 리로드** ← 필수
   봇은 `lru_cache`라 실행 중이면 옛 데이터를 물고 있음.
   - Slack에서 `리로드`(또는 `reload`) 명령, **또는** 프로세스 재시작

---

## 검증 (리로드 후)
- **BL 트렌드** → 40편, 6축(장르/배경·소재·관계·남주·분위기, 여주는 BL이라 비움) 값이 떠야 함
- **일반 트렌드** → 92편. `혐관`·`집착남`·`오피스로맨스`·`신분숨김` 등 트로프 필터에 결과 나와야 함
- 안 뜨면: 사본이 옛 버전(97편)이거나 `bl_cats.json` 누락 → 2번 재실행 + 3번 리로드

---

## 주의/한계
- **신뢰도 게이트** `MIN_CONF=0.6` (`bot/trend_search.py`). 자동편 5개는 저신뢰라 여전히 제외됨(의도된 품질 게이트).
- **트로프 필터 커버 86/92** — 관계·소재·남주가 다 빈 6편은 트렌드 카운트엔 들지만 특정 트로프 값은 없음.
- `trope_tags_ko`/`catharsis_type`는 `cats` 역산값(근사). 원조 v4 편의 기존 값은 그대로 둠.
- BL은 `trope_tags_ko`가 아니라 `bl_tags`(6축)로 필터됨 — 일반 트로프 필터와 어휘 체계가 다름.

---

# 지시문 — 봇 트렌드 "성향별 계층화" 변경 고지 (2026-07-09)

> **이 봇의 트렌드 검색을 변경합니다. 무엇을 바꾸는지 아래로 확인하세요.**
>
> **바꾸는 것**: 기존엔 BL만 별도 트렌드였고 나머지(서양 남녀 클립 94편)는 트렌드에 안 잡혔습니다.
> 이제 뷰어와 동일하게 **상위계층(성향 BL/GL/남녀 · 국가 한국/서양 · 플랫폼 틱톡/유튜브)**으로 먼저
> 스코프를 잡고, 그 버킷 **안에서만 cats 6축(분위기·남주·여주·관계·소재·장르배경)** 트렌드를 집계합니다.
>
> **왜**: 서양 틱톡 실사 클립과 한국 유튜브 AI-BL은 소스·지표 스케일이 달라, 한 통에 합치면 트렌드가
> "소스 차이"로 왜곡됩니다. 상위축으로 먼저 나누면 같은 성격끼리만 비교돼 섞이지 않습니다.
>
> **동작(예시 질문)**: `남녀 트렌드`(94) · `BL 트렌드`(42) · `GL 트렌드`(1) ·
> `서양 남녀 트렌드` · `한국 유튜브 BL`(40) — 국가/플랫폼을 붙이면 하위로 좁혀집니다.
>
> **적용 조건**: 코드는 반영됨(`bot/trend_search.py`). **봇 실행 중이면 `리로드` 또는 재시작** 필요.
> 데이터에 `orient`(성향) 필드가 있어야 하며, 최신 `reference_db.json` 동기화본에 포함돼 있습니다.

## 관련 커밋
- story-v1-scripts: `4ecc7cf`(백필) · `b93c4f1`(orient 저장)
- co-writer-bot: `af69d0a`(사본 동기화) · `cd3aa88`(성향별 cats 트렌드 + 재동기화)
